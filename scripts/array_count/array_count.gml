function array_count(_array, _value)
{	
	//count how many of an item there is
	var n = 0;
	for(var i = 0; i < array_length(_array);i++)
	{
	    if _array[i] = _value
	    {
	        n++;
	    }
	}    
	return n;	
}